name: governance-lints

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Rule 3 diffs against a base ref and reads file content at that ref.
          # A shallow clone has neither.
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Resolve base ref
        id: base
        run: |
          BASE="${{ github.event.pull_request.base.sha || github.event.before }}"
          # A first push to a branch, or a force-push, can yield an unusable ref.
          if [ -z "$BASE" ] || ! git cat-file -e "${BASE}^{commit}" 2>/dev/null; then
            BASE=$(git rev-parse HEAD~1 2>/dev/null || echo "")
          fi
          echo "ref=$BASE" >> "$GITHUB_OUTPUT"
      - name: Check template versions
        run: |
          if [ -n "${{ steps.base.outputs.ref }}" ]; then
            node scripts/check-template-versions.mjs --base "${{ steps.base.outputs.ref }}"
          else
            # No usable base — rules 1-2 still run, and we say so rather than
            # reporting a pass that quietly skipped the rule that matters.
            echo "::warning::No usable base ref; rule 3 (version bump on change) did not run."
            node scripts/check-template-versions.mjs
          fi
      - name: Test the lints
        # This repo's coverage floor is a rule, not a percentage: every script in
        # scripts/ fires on a known-bad input and clears on a known-good one. Two
        # lint bugs have shipped here, both of the only kind that matters — a
        # check that quietly stopped checking, indistinguishable in CI from one
        # that passed. See docs/testing-strategy.md.
        # Unquoted so bash expands the glob. `node --test 'glob'` is Node 22+;
        # this job pins Node 20, where the quoted form is read as a literal path.
        run: node --test test/*.test.mjs

      - name: Check decision-record indexes
        # Every NNN-*.md in docs/adr/ and docs/pdr/ is registered in its README.
        run: node scripts/check-adr-readme-sync.mjs

      - name: Check PDR falsifiers
        # A decision without a falsifier is a wish. Gates on a missing falsifier
        # and on the phrasings the form rules out; reports the heuristic half and
        # any revisit date that has come due. See docs/pdr/README.md.
        run: node scripts/check-pdr-falsifiers.mjs

      - name: Check blank-form naming
        # A blank form in a records directory gets read as a record — twice here,
        # by two different readers, from the same root cause.
        run: node scripts/check-blank-form-naming.mjs

      - name: Check /analyze-repo template coverage
        # Every template must be offered by the bootstrap command or excluded on
        # the record. A bootstrap that omits templates looks like one that worked.
        run: node scripts/check-analyze-repo-coverage.mjs

      - name: Check mothership drift
        # This repo enforces template sync in every governed repo; this is the check
        # on its own installed copies. The register (docs/mothership-drift-register.md)
        # names the docs/ files required byte-identical to their templates; every other
        # name collision must be registered or exempted on the record. Pure repo-local
        # reads — unlike the two ledger-reading scripts, this one genuinely runs in CI.
        run: node scripts/check-mothership-drift.mjs

      - name: Check enforcement stanzas
        # A permission stanza cannot report its own absence. Asserts this repo's
        # harness configs (.claude/settings.json, opencode.json) carry the stamped,
        # complete enforcement stanza for every records path in
        # docs/enforcement-stanzas-register.md — and that every records file named
        # in CLAUDE.md is in that register (blocking UNREGISTERED otherwise).
        run: node scripts/check-enforcement-stanzas.mjs
