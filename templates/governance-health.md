<!-- [PROPOSED from source repo] New template — review before committing.

This is the shape of the auto-generated docs/governance-health.md, abstracted from
the source repo's live implementation after 8 audit cycles. Your governance-health
script regenerates this file as part of each audit (and may also print it report-only
in the dead-man probe's job log). See docs/governance-health-spec.md in repo-governance
for the implementation brief.

Where this differs from the original spec, the live implementation won:
- Resolution time covers ALL resolved findings with date-prefixed IDs, not just
  trailing-30-day P1s — small repos need every data point; the P1 p50/p90 summary
  line carries the target.
- The Trend table gained Commits and Score/commit columns — raw failure score is
  meaningless across gaps in audit cadence; normalizing by commits in the window
  makes a 12-day backlog comparable to a 1-day cycle.
- The script prints data-quality warnings to stderr when inputs are degraded
  (short finding IDs, shallow clone, no `gh` auth); warnings appearing in an audit
  PR are themselves audit findings.

Don'ts (from the spec, proven in practice):
- Don't track individual developer velocity with these metrics.
- Don't expose raw scores to clients — report trends.
- Don't set targets before you have 3+ data points; calibrate first.
-->

# Governance Health

Last updated: [DATE] (auto-generated by `[your governance-health script]`)

## Trend

| Audit date | P0 new | P1 new | P2 new | Failure score | Commits | Score/commit |
|---|---|---|---|---|---|---|
| YYYY-MM-DD | 0 | 0 | 0 | 0 | — | — |

Failure score formula: `(P0_new × 3) + (P1_new × 2) + (P2_new × 1)`

<!-- Proxy for DORA change failure rate: weighted new findings per audit cycle.
     Score/commit normalizes across uneven audit cadence. -->

## Resolution time (all resolved findings with date-prefixed IDs)

| Finding | First seen | Resolved | Days |
|---|---|---|---|
| `AUDIT-YYYY-MM-DD-P1-NN` | YYYY-MM-DD | YYYY-MM-DD | 0 |

P1 p50 MTTR: N days | P1 p90 MTTR: N days
_Target: p50 ≤ 7 days (one sprint)_

<!-- Proxy for DORA MTTR. Requires the full finding-ID convention
     (AUDIT-YYYY-MM-DD-<severity-or-domain>-NN) in audit docs — the date prefix
     is the "first seen" timestamp. -->

## Deployment frequency

PRs merged/week (trailing 4 weeks): N

<!-- DORA deployment frequency via `gh pr list`. The script should degrade
     gracefully: print "_GitHub metrics unavailable — run with gh auth active._"
     rather than failing the audit. -->

## Lead time

PR open-to-merge: p50 N days / p90 N days (trailing 4 weeks)

## Signal

Failure rate: [trending down ✓ | flat | trending up ⚠]
P1 MTTR: [healthy ✓ | watch | over target ⚠]

<!-- One-line interpretation written by the script. Review at the monthly
     retrospective — not per-PR. These measure the governance system, not people. -->
